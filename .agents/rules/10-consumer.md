# Rule: Consumer
Build only the smallest consumer needed to prove the API works externally:
- One page.
- Data list/display.
- Filter control.
- Next-page control.

It must call the public API URL.

For excellent-level completion, implement explicit empty and error states.
