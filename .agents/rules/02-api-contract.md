# Rule: REST API Contract
- Use plural resource nouns.
- Use HTTP methods for operations.
- Prefix all API paths with `/api/v1/`.
- Provide collection and item endpoints for resources.
- Use nested resources where required by the chosen domain.
- Keep request/response contracts consistent.
- Do not return 200 for an error.
- Preserve the documented response envelope across endpoints.
