# Rule: Deployment
- API must be publicly reachable.
- Use managed Postgres.
- Configure environment variables correctly.
- Run the seed against production.
- Verify from outside the local environment.
- Consumer must call the deployed public URL, not localhost.
